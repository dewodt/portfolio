---
import GitHubIcon from "@/components/icons/GitHubIcon.astro";
import GitLabOutlineIcon from "@/components/icons/GitLabOutlineIcon.astro";

interface Props {
  repository: {
    url: string;
    label: string;
  };
}

const { repository } = Astro.props;
---

<li>
  <a
    href={repository.url}
    target="_blank"
    class="group flex flex-row items-center gap-2"
  >
    {
      repository.url.startsWith("https://gitlab.com") ? (
        <GitLabOutlineIcon class="stroke-muted group-hover:stroke-secondary size-5 lg:size-6" />
      ) : (
        <GitHubIcon class="fill-muted group-hover:fill-secondary size-5 lg:size-6" />
      )
    }

    <span
      class="text-muted group-hover:text-secondary text-base leading-none font-medium underline-offset-4 group-hover:underline lg:text-lg lg:leading-none"
    >
      {repository.label}
    </span>
  </a>
</li>
