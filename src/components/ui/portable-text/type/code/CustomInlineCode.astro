---
import { runHighlighterWithAstro } from "@astrojs/prism/dist/highlighter"; // Create code manually, without <pre />

// Props
interface Props {
  language: string;
  code: string;
}

const { language, code } = Astro.props;
const { classLanguage, html } = runHighlighterWithAstro(language, code);
---

<code class={classLanguage} set:html={html} />
