---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "flex flex-row items-center gap-2 rounded-full border border-border bg-card px-4 py-2 text-sm font-semibold leading-none text-secondary transition-all duration-200 hover:border-transparent hover:bg-primary/35",
    className,
  ]}
>
  <slot />
</div>
