---
import { Image } from "astro:assets";

interface Props {
  src: string;
  alt: string;
  class?: string;
}

const { src, alt, class: className } = Astro.props;
---

<div
  class:list={[
    "flex aspect-square size-12 shrink-0 items-center justify-center overflow-hidden rounded-lg bg-white lg:size-14",
    className,
  ]}
>
  <Image
    class="text-secondary h-auto w-full object-contain text-xs font-medium"
    src={src}
    alt={alt}
    widths={[48, 56, 96, 112]}
    sizes="(max-width: 1023px) 48px, 56px"
    inferSize
  />
</div>
